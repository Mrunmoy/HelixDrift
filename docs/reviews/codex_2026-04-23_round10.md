}
```

Threshold: 10 ms. (Below: normal RTT variance. Above: architectural
glitch.)

Risks:
- **Sustained real drift rejected.** If Tag's clock actually
  drifts 10+ ms in one interval (crystal failure, thermal shock),
  the glitch-reject would prevent re-lock. Mitigation: count
  consecutive rejections; after N in a row, accept the new
  baseline.
- **Initial lock.** First anchor after boot always "jumps" by a
  huge amount from the initial zero offset. Already handled —
  `midpoint_offset_valid == 0` on first sample, we ALWAYS accept.
- **Hub reboot mid-stream.** Would reset Hub clock; Tag sees
  dramatic jump. Handled the same way as initial lock — on
  repeated rejection, re-baseline after N hits.

## Expected effect

If hypothesis is right, glitch-reject should:
- Drop per-Tag `|err|` p99 from ~10 ms to **~3 ms** (just the
  2-10 ms bucket).
- Drop cross-Tag span p99 from ~42 ms to **~5 ms** (no more
  fat-tail events pulling one Tag away from fleet median).

If observed effect is smaller, the fat-tail is not primarily
driven by midpoint jumps — maybe hypothesis (3) radio glitches.

## Action

Not fixing autonomously — per user's sprint directive:
1. Reviewers will sanity-check this diagnosis + fix proposal.
2. If agreed, land glitch-reject as v22.
3. Measure. Commit findings.

Tool: `tools/analysis/find_span_outliers.py` (committed alongside
this doc). Re-run against any future capture to check if cross-Tag
outliers persist.

codex
Q1: mostly yes, but I’d phrase it as “slow relative wander” rather than fully proven static drift. The failed v22 intervention is strong negative evidence against “rare single-sample jumps are the dominant cause.” The uniform participation across healthy Tags and the similar p50/p99 inflation both fit low-frequency per-Tag offset wander. One caveat: span is an extreme-value metric across multiple Tags, so ratio-preserved scaling alone is suggestive, not dispositive.

Q2: PC-side correction is the right architectural direction, but I would not stop at a one-time fixed bias table. Your own description says the bias drifts. So the winning path is hub-authoritative time plus a slow per-Tag bias tracker on the PC: calibrate an initial offset, then update it with a low-bandwidth estimator against hub or fleet median. Firmware’s job should be to provide stable raw observability and lock-quality signals, not to overfit correction logic on-device.

Q3: yes, initial lock should be a quality-gated acquisition state, not “first sample wins.” I would use:
1. Collect candidate midpoints into a short window.
2. Require `N=4` or `5` samples whose spread is within tolerance.
3. Commit the lock to the median of that cluster, not the last sample.
For tolerance, start around `2-3 ms` absolute spread, or derive it from your healthy `mid_step` distribution and set it near the top of the normal cluster, not at the 10 ms glitch threshold. Also add a re-acquire path if recent accepted samples become bimodal or the cluster disappears.

Q4: EMA on midpoint is low value as a primary defense. It attenuates noise but cannot separate good from bad samples, so it just smears contamination over time and slows recovery. I would avoid EMA on the lock baseline. If you use EMA anywhere, use it only in the PC-side slow bias tracker after quality gating.

Q5: the surprising part is that v22 exposed a measurement blind spot: a Tag can be catastrophically wrong yet still look “clean” in p99 once it is stably wrong. That means you need lock-health metrics beyond `|err|` percentiles: acquisition age, recent candidate spread, relock count, fraction of samples in the dominant cluster, and distance from fleet median.

Next experiment: run an offline replay on a good v21 capture. For each Tag, subtract:
1. a fixed per-Tag bias, then
2. a rolling 5-10 s median bias relative to fleet median.
If cross-Tag span p99 collapses only with the rolling correction, you’ve confirmed drift/wander rather than jump-driven tails, and you’ve validated the PC architecture before touching firmware again.
tokens used
26,773
Q1: mostly yes, but I’d phrase it as “slow relative wander” rather than fully proven static drift. The failed v22 intervention is strong negative evidence against “rare single-sample jumps are the dominant cause.” The uniform participation across healthy Tags and the similar p50/p99 inflation both fit low-frequency per-Tag offset wander. One caveat: span is an extreme-value metric across multiple Tags, so ratio-preserved scaling alone is suggestive, not dispositive.

Q2: PC-side correction is the right architectural direction, but I would not stop at a one-time fixed bias table. Your own description says the bias drifts. So the winning path is hub-authoritative time plus a slow per-Tag bias tracker on the PC: calibrate an initial offset, then update it with a low-bandwidth estimator against hub or fleet median. Firmware’s job should be to provide stable raw observability and lock-quality signals, not to overfit correction logic on-device.

Q3: yes, initial lock should be a quality-gated acquisition state, not “first sample wins.” I would use:
1. Collect candidate midpoints into a short window.
2. Require `N=4` or `5` samples whose spread is within tolerance.
3. Commit the lock to the median of that cluster, not the last sample.
For tolerance, start around `2-3 ms` absolute spread, or derive it from your healthy `mid_step` distribution and set it near the top of the normal cluster, not at the 10 ms glitch threshold. Also add a re-acquire path if recent accepted samples become bimodal or the cluster disappears.

Q4: EMA on midpoint is low value as a primary defense. It attenuates noise but cannot separate good from bad samples, so it just smears contamination over time and slows recovery. I would avoid EMA on the lock baseline. If you use EMA anywhere, use it only in the PC-side slow bias tracker after quality gating.

Q5: the surprising part is that v22 exposed a measurement blind spot: a Tag can be catastrophically wrong yet still look “clean” in p99 once it is stably wrong. That means you need lock-health metrics beyond `|err|` percentiles: acquisition age, recent candidate spread, relock count, fraction of samples in the dominant cluster, and distance from fleet median.

Next experiment: run an offline replay on a good v21 capture. For each Tag, subtract:
1. a fixed per-Tag bias, then
2. a rolling 5-10 s median bias relative to fleet median.
If cross-Tag span p99 collapses only with the rolling correction, you’ve confirmed drift/wander rather than jump-driven tails, and you’ve validated the PC architecture before touching firmware again.
